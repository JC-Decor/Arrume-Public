@{
    ViewData["Title"] = "Obrigado";
    Layout = "_Layout";
}

<section class="thanks">
    <div class="container">
        <div class="card center thanks__card">
            <div class="check-badge" aria-hidden="true">✓</div>
            <h2>Obrigado!</h2>
            <p class="muted">Recebemos seu pedido. Em breve você receberá no WhatsApp as opções de capoteiros/estofadores.</p>
            <a href="/" class="btn secondary">Voltar</a>
        </div>
    </div>
</section>

@section Scripts {
<script>
  // GA4: evento de conversão (lead gerado)
  if (window.gtag) {
    gtag('event', 'generate_lead', {
      value: 1
      // parâmetros adicionais opcionais (currency, campaign etc.)
    });
  }

  // Meta Pixel: evento de Lead
  if (window.fbq) {
    fbq('track', 'Lead');
  }
</script>
}
